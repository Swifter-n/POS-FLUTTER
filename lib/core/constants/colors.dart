import 'package:flutter/material.dart';

class AppColors {
  // Primary - Teal
  static const Color primary = Color(0xFF0DA487);
  static const Color primaryDark = Color(0xFF006D5B);
  static const Color primaryLight = Color(0xFFE7F6F3);

  // Secondary - Dark Grey/Black
  static const Color secondary = Color(0xFF2D2D2D);

  // Tertiary - Mustard Gold
  static const Color tertiary = Color(0xFFFBCA1B);

  // Neutral - Backgrounds
  static const Color background = Color(0xFFF0F3F5);
  static const Color card = Colors.white;
  static const Color stroke = Color(0xFFE0E0E0);

  // Text Colors
  static const Color textPrimary = Color(0xFF2D2D2D);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textLight = Colors.white;

  // Additional status from image
  static const Color red = Color(0xFFC62828); // For delete icon
}
